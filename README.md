# Villa Azure React

React + Vite + TypeScript port of the Villa Azure website.

## Scripts

```bash
npm install
npm run dev
npm run build
npm run lint
```

The contact form submits to a Google Apps Script Web App URL set via `VITE_CONTACT_FORM_URL`.
